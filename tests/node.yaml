config:
  environments:
    local:
      target: "http://192.168.49.2:31000"
      phases:
        - duration: 30
          arrivalRate: 50
scenarios:
  - flow:
    - log: "Current environment is set to: {{ $environment }}"